header
  a(href="/")
    h1.logo
      span.logoPart1 Whisper
      span.logoPart2 Wave
  div.searchDiv
    form(action="" method="post") 
      label(for="search")
      input(type="text" name="search" placeholder='search')
    img.searchImg(src="images/search.svg" alt='loupe icon')
  nav
    a.login(href="/login")
      strong Login
